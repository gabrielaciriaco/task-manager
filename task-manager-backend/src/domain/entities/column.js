class Column {
  constructor({ title, createdAt, updatedAt, id, cards }) {
    this.title = title
    this.createdAt = createdAt
    this.updatedAt = updatedAt
    this.id = id
    this.cards = cards
  }
}

export default Column
